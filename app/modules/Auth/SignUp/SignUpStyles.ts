import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textIn: {
    borderWidth: 1,
    height: 40,
    marginBottom: 15,
    marginTop: 5,
    paddingLeft: 5,
  },

  inputTitle: {
    fontWeight: '500',
  },

  btn: {
    width: 100,
    height: 40,
    justifyContent: 'center',
    alignItems: 'center',
  },

  rowStyle: {
    marginTop: 10,
    alignItems: 'center',
  },

  signUp: {
    color: 'blue',
  },
});
